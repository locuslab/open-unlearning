# @package _global_

defaults:
  - _self_
  - model: Llama-3.2-3B-Instruct
  - eval: tofu
  - paths: default
  - hydra: eval
  - experiment: null

# Note: model.model_args.device_map is set via CLI or model config
# Do not override model_args here as it replaces the entire dict

mode: eval
task_name: ???
seed: 0