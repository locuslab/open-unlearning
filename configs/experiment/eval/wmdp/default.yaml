# @package _global_

defaults:
  - override /model: Llama-3.2-1B-Instruct
  - override /eval: wmdp

forget_split: cyber-forget-corpus
retain_logs_path: null

model:
  model_args:
    pretrained_model_name_or_path: open-unlearning/wmdp_Llama-3.2-1B-Instruct_full

eval:
  wmdp:
    forget_split: ${forget_split}
    retain_logs_path: ${retain_logs_path}

task_name: wmdp_unlearn
